export type Coordinates = {
  x: number;
  y: number;
};

export type Vector = {
  x: number;
  y: number;
};

export type DirectionVector = {
  x: -1 | 0 | -0 | 1;
  y: -1 | 0 | -0 | 1;
};

export type ConsecutiveNumber = {
  digit: number;
  occurence: number;
};
