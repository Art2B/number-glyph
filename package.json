{
  "name": "parcel-vanilla-starter",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "prestart": "rm -rf .parcel-cache dist",
    "start": "parcel",
    "build": "parcel build  --public-url https://art2b.github.io/number-glyph/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.0"
  }
}
